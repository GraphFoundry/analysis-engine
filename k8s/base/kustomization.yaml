apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: what-if-simulation-engine

resources:
  - deployment.yaml
  - service.yaml

commonLabels:
  app.kubernetes.io/name: what-if-simulation-engine
  app.kubernetes.io/component: simulation
  app.kubernetes.io/part-of: adaptive-microservice-management

# Image configuration (override in overlays)
images:
  - name: what-if-simulation-engine
    newTag: latest
