apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: predictive-analysis-engine

resources:
  - deployment.yaml
  - service.yaml

commonLabels:
  app.kubernetes.io/name: predictive-analysis-engine
  app.kubernetes.io/component: simulation
  app.kubernetes.io/part-of: adaptive-microservice-management

# Image configuration (override in overlays)
images:
  - name: predictive-analysis-engine
    newTag: latest
