apiVersion: v1
kind: Service
metadata:
  name: what-if-simulation-engine
  labels:
    app: what-if-simulation-engine
    component: simulation
spec:
  type: ClusterIP
  selector:
    app: what-if-simulation-engine
  ports:
    - name: http
      port: 7000
      targetPort: http
      protocol: TCP
